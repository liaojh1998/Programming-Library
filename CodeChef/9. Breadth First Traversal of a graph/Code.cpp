#include <bits/stdc++.h>
using namespace std;

//Pseudocode:
void bfs(){
	//queue<state> q;
	//q.push(initial state);
	//while(!q.empty()){
	//	state = q.front(); q.pop();
	//	set current state as visited for no repeats
	//	modify next states from current state and push them into q
	//}
}

//Pseudocode:
void floodfill(){
	//for(int i = 0; i < y; i++)
	//	for(int j = 0; j < x; j++)
	//		if(!filled[y][x])
	//			bfs(state at (y, x));
	//Use data for whatever during bfs process or after bfs process
}

int main(){
	//Enter code here
	return 0;
}